# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'windows-latest'

steps:
- task: HugoTask@1
  inputs:
    source: '$(Build.SourcesDirectory)'
    destination: '$(Build.SourcesDirectory)\public'
    baseURL: 'https://updated.sigmact.com'

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.SourcesDirectory)\public'
    ArtifactName: 'drop'
    publishLocation: 'Container'

# - task: AzureFileCopy@4
#   inputs:
#     SourcePath: '$(Build.SourcesDirectory)\public\'
#     azureSubscription: 'シグマコンサルティング(eee9046c-f7c0-4e03-98e0-3660ae9224fc)'
#     Destination: 'AzureBlob'
#     storage: 'sigmactwwwupdated'
#     ContainerName: 'test'

- task: AzureCLI@2
  displayName: 'Upload Prod'
  inputs:
    # ServiceConnectorにて設定
    azureSubscription: "シグマコンサルティング(eee9046c-f7c0-4e03-98e0-3660ae9224fc)"
    scriptType: "bash"
    scriptLocation: "inlineScript"
    inlineScript: 'az storage blob upload-batch -d /$web -s /$(Build.SourcesDirectory)\public --account-name sigmactwwwupdated --account-key eyrNJLPkRQzcNmSyyFZntcH6SSJvUXYy8+eDWnL58fsoNROz/+4TCTsizrJ1CRW2torwwLGAa6FSndaEKDMJNQ=='
